# Used by Vite proxy target only (server-side — NOT exposed to browser via import.meta.env)
# Keep VITE_API_URL blank so api.ts uses relative paths that go through the proxy
VITE_API_URL=
BACKEND_URL=http://localhost:5000
