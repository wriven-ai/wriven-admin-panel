# Used by Vite proxy target only (server-side — NOT exposed to browser via import.meta.env)
# API paths are prefixed with /v1 in src/lib/api.ts and by the proxy itself
VITE_API_URL=https://api.wriven.tech
BACKEND_URL=https://api.wriven.tech

# Demo login — shown as a quick-access button on the login screen
VITE_DEMO_ADMIN_EMAIL=anowar@wriven.com
VITE_DEMO_ADMIN_PASSWORD=wrAnowar@123
